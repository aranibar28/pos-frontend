<form>
    <mat-form-field>
      <input type="text" placeholder="Selecciona un producto" aria-label="Producto" matInput [formControl]="productControl" [matAutocomplete]="auto">
      <mat-autocomplete #auto="matAutocomplete" [displayWith]="displayFn">
        <mat-option *ngFor="let product of filteredProducts | async" [value]="product">
          {{ product.description }}
        </mat-option>
      </mat-autocomplete>
    </mat-form-field>
    <button mat-raised-button (click)="addToShoppingCart()" type="button"> Añadir al carrito </button>
  </form>
  <table mat-table [dataSource]="shoppingCart">
    <!-- Description Column -->
    <ng-container matColumnDef="description">
      <th mat-header-cell *matHeaderCellDef> Descripción </th>
      <td mat-cell *matCellDef="let product"> {{product.description}} </td>
    </ng-container>

    <!-- Price Column -->
    <ng-container matColumnDef="price">
      <th mat-header-cell *matHeaderCellDef> Precio </th>
      <td mat-cell *matCellDef="let product"> {{product.price}} </td>
    </ng-container>

    <!-- Quantity Column -->
    <ng-container matColumnDef="quantity">
      <th mat-header-cell *matHeaderCellDef> Cantidad </th>
      <td mat-cell *matCellDef="let product">
        <button mat-icon-button (click)="incrementQuantity(product)" type="button">
          <mat-icon>add</mat-icon>
        </button>
        {{product.quantity}}
        <button mat-icon-button (click)="decrementQuantity(product)" type="button">
          <mat-icon>remove</mat-icon>
        </button>
      </td>
    </ng-container>

    <!-- Subtotal Column -->
    <ng-container matColumnDef="subtotal">
      <th mat-header-cell *matHeaderCellDef> Subtotal </th>
      <td mat-cell *matCellDef="let product"> {{product.price * product.quantity}} </td>
    </ng-container>

    <!-- Remove Column -->
    <ng-container matColumnDef="remove">
        <th mat-header-cell *matHeaderCellDef></th>
        <td mat-cell *matCellDef="let product">
          <button mat-icon-button (click)="removeFromShoppingCart(product)">
            <mat-icon>delete</mat-icon>
          </button>
        </td>
      </ng-container>

      <tr mat-header-row *matHeaderRowDef="displayedColumns"></tr>
      <tr mat-row *matRowDef="let row; columns: displayedColumns;"></tr>
</table>

<p>Total: {{getTotal()}}</p>
<p>Productos: {{getTotalQuantity()}}</p>